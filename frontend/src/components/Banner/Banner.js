import { Container } from "@mui/material";
import React from "react";

const Banner = () => {
  return (
    <Container
      disableGutters={true}
      maxWidth={false}
      className="banner"
    ></Container>
  );
};

export default Banner;
